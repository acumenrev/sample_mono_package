import XCTest
@testable import ios_private_package

final class ios_private_packageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ios_private_package().text, "Hello, World!")
    }
}
